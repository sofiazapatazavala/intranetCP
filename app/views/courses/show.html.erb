<div class="row">
  <div class="col-md-8 col-lg-9 col-xl-10 mb-3 mb-md-0">
    <h3 class="p-2 p-cursos text-white">🏫 Detalle de Curso</h3>
    <p class="lead"><%= @course.nombre %></p>
    <%= link_to 'Agregar nueva alumna', new_student_path, class: "btn btn-block btn-outline-primary", role: "button" %>
  </div>
  <div class="col-md-4 col-lg-3 col-xl-2">
    <div class="card">
      <div class="card-block">
        <h5 class="card-subtitle text-center hidden-md-up">Acciones:</h5>
        <div class="d-flex justify-content-center">
          <div class="btn-group-vertical" role="group" aria-label="Acciones para esta clase">
            <%= link_to '⬅ Volver', courses_path, class: "btn btn-secondary", role: "button" %>
            <%= link_to '✏ Editar curso', edit_course_path(@course), class: "btn btn-warning", role: "button" %>
            <%= link_to '🗑 Borrar', @course, class: "btn btn-danger", method: :delete, data: { confirm: '¿Segura?' } %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<hr>

<p id="notice"><%= notice %></p>

<div class="card">
  <div class="card-block">
    <ul class="list-group">
      <h2 class="card-title text-center"><%= @course.nombre %></h2>
      <li class="list-group-item">Estilo: <%= @course.estilo %></li>
      <li class="list-group-item">Edades: <%= @course.edades %></li>
    </ul>
    <p class="lead">Fotos del curso</p>
    <div class="card-group">
      <% @course.photos.each do |photo| %>
        <div class="card">
          <%= image_tag photo, class: "card-img-top" %>
        </div>
      <% end %>
      </div>
  </div>
</div>

<div class="card">
  <div class="card-block">
    <h4>Información sobre alumnas:</h4>
    <div class="table-responsive">
      <table class="table">
        <tr>
          <th>Nombre</th>
          <th>E-mail</th>
          <th>Situación de pago</th>
          <th colspan="3"></th>
        </tr>
        <% @course.students.each do |student| %>
        <tr>
          <td><%= student.nombre %></td>
          <td><%= student.email %></td>
          <td></td>
          <td><%= link_to "Ver", student, class: "btn btn-block btn-info", role: "button" %></td>
          <td><%= link_to "Editar", edit_student_path(student), class: "btn btn-block btn-primary", role: "button" %></td>
          <td><%= link_to 'Borrar', student, class: "btn btn-block btn-danger", method: :delete, data: { confirm: '¿Segura?' } %></td>
        </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>

<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>
