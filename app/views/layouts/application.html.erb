<!DOCTYPE html>
<html>
  <head>
    <title>Constanza Prieto - Intranet</title>

    <!-- Required meta tags always come first -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <!-- Meta tags para el funcionamiento como web app en iOS -->
    <%= favicon_link_tag 'apple-touch-icon.png', rel: 'apple-touch-icon', type: 'image/png' %>
    <meta name="apple-mobile-web-app-title" content="Constanza Prieto">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <!-- manifest.json para Android -->
    <%= tag(:link, rel: 'manifest', href: asset_url('manifest.json')) %>

    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <% if user_signed_in? %>
      <div class="row">
        <div class="col-12">
          <div class="collapse" id="navbarSuperior">
            <div class="text-muted p-1">
              <div class="row">
                <div class="col-md-8">
                  <%= link_to 'Inicio', root_path, class: "btn btn-lg btn-secondary", role: "button" %>
                </div>
                <div class="col-md-4">
                  <%= link_to 'Cerrar Sesión', destroy_user_session_path, method: :delete, class: "btn btn-block btn-lg btn-outline-danger", role: "button" %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      <% end %>
      <div class="row">
        <div class="col-12">
          <div class="nav-CP-logo mb-3">
            <a role="button" data-toggle="collapse" data-target="#navbarSuperior" aria-controls="navbarSuperior" aria-expanded="false" aria-label="Ver más navigation"><%= image_tag 'logoCP-head.png', :class => "img-fluid d-block mx-auto" %></a>
          </div>
        </div>
      </div>
      <div class="row">
        <div class="col-12">
          <%= notice %>
          <%= alert %>
        </div>
      </div>
      <div class="row">
        <div class="col-12">
          <%= yield %>
        </div>
      </div>
    </div>
    <% if user_signed_in? %>
    <nav class="navbar fixed-bottom nav-CP">
      <div class="container">
      <div class="row">
        <div class="col">
          <%= link_to '👰🏻', weddings_path %>
        </div>
        <div class="col">
          <%= link_to '🏫', courses_path %>
        </div>
        <div class="col">
          <%= link_to '📆', agenda_path %>
        </div>
        <div class="col">
          <%= link_to '👯', couples_path %>
        </div>
        <div class="col">
          <%= link_to '💃🏻', special_classes_path %>
        </div>
      </div>
      </div>
    </nav>
    <% end %>
  </body>
</html>
